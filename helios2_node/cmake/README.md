# Place Findarena_sdk.cmake here

This package reuses the Arena SDK find module from `arena_camera_node`.

Copy it in before building:

```bash
cp ../arena_camera_node/cmake/Findarena_sdk.cmake .
```

(Run from `~/cobot_ws/src/helios2_node/cmake/`)
